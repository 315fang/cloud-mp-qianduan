/* pages/address/list.wxss */
.container {
  min-height: 100vh;
  padding-bottom: 140rpx;
}

.address-list {
  padding: 20rpx 24rpx;
}

.address-card {
  position: relative;
  background: #fff;
  border-radius: 16rpx;
  padding: 28rpx 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
  border: 2rpx solid transparent;
}
.address-card.selectable {
  position: relative;
  cursor: pointer;
}

.address-card.selectable .address-main {
  padding-right: 64rpx;
}

.address-card.selected .address-main {
  padding-right: 64rpx;
}

.address-card.selectable:active {
  background: #EFF6FF;
  border-color: #BFDBFE;
}

.address-card.selected {
  background: #F0FDF4;
  border-color: #22C55E;
  box-shadow: 0 8rpx 24rpx rgba(34, 197, 94, 0.14);
}

.address-card.selected .address-name {
  color: #166534;
}

.address-card.default-active .address-tag {
  color: #15803D;
  border-color: #22C55E;
  background: #DCFCE7;
}

/* 选择模式顶部提示 */
.select-tip {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 20rpx 28rpx;
  background: #EFF6FF;
  border-bottom: 1rpx solid #DBEAFE;
}
.select-tip-icon { width: 32rpx; height: 32rpx; opacity: 0.6; }
.select-tip-text { font-size: 26rpx; color: var(--brand-blue-deep); font-weight: 500; }

/* 选择模式箭头 */
.select-arrow {
  position: absolute;
  right: 24rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 40rpx;
  color: #CBD5E1;
  line-height: 1;
}

.selected-mark {
  position: absolute;
  right: 24rpx;
  top: 28rpx;
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background: #22C55E;
  color: #fff;
  font-size: 26rpx;
  line-height: 40rpx;
  text-align: center;
  font-weight: 600;
}

/* 卡片滑出动画 */
.address-card.deleting {
  transform: translateX(-100%);
  opacity: 0;
  height: 0;
  margin: 0;
  padding: 0;
  transition: all 0.3s ease;
}

.address-main {
  margin-bottom: 20rpx;
}

.address-top {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.address-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #0F172A;
}

.address-phone {
  font-size: 28rpx;
  color: #64748B;
}

.address-tag {
  font-size: 20rpx;
  color: var(--brand-blue-deep);
  border: 1rpx solid var(--brand-blue-deep);
  border-radius: 6rpx;
  padding: 2rpx 12rpx;
}

.address-selected-tag {
  font-size: 20rpx;
  color: #15803D;
  background: #DCFCE7;
  border-radius: 6rpx;
  padding: 3rpx 12rpx;
}

.address-detail {
  font-size: 26rpx;
  color: #334155;
  line-height: 1.6;
}

.address-actions {
  display: flex;
  justify-content: flex-end;
  gap: 24rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #F1F5F9;
}

.action-btn {
  font-size: 24rpx;
  color: #64748B;
  padding: 8rpx 16rpx;
}

.action-btn.danger {
  color: #EF4444;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 200rpx;
}

/* WXML 中使用 empty-icon-img，需同时兼容 empty-icon */
.empty-icon,
.empty-icon-img {
  width: 96rpx;
  height: 96rpx;
  opacity: 0.35;
  margin-bottom: 20rpx;
}

.empty-text {
  font-size: 30rpx;
  color: #334155;
  margin-bottom: 8rpx;
}

.empty-sub {
  font-size: 24rpx;
  color: #94A3B8;
}

.loading-state {
  text-align: center;
  padding: 100rpx 0;
  color: #94A3B8;
  font-size: 28rpx;
}

/* 底部添加按钮 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20rpx 24rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  box-shadow: 0 -2rpx 20rpx rgba(0, 0, 0, 0.05);
}

.add-btn {
  background: linear-gradient(135deg, #0F172A 0%, #1E293B 100%);
  color: #fff;
  text-align: center;
  padding: 24rpx 0;
  border-radius: 48rpx;
  font-size: 30rpx;
  font-weight: 500;
}
